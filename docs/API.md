<!--
SPDX-FileCopyrightText: 2026 M. Kasim Doenmez
SPDX-License-Identifier: Apache-2.0
-->

<!--
This file is generated from docs/MACHINE_READABLE_API.json.
Do not edit this file by hand.

Regenerate with:

    python tools/generate_api_md.py

Check without modifying files:

    python tools/generate_api_md.py --check
-->
# API Inventory

This file is generated from `docs/MACHINE_READABLE_API.json`.

Do not edit this file by hand. Update `docs/MACHINE_READABLE_API.json` and regenerate this Markdown file instead.

`docs/MACHINE_READABLE_API.json` is the authoritative machine-readable API inventory. This file is a human-readable subset intended for browsing and review.

This document describes public-facing Rust APIs provided by the SAR reference implementation. It is not a protocol specification, security audit, stability guarantee, production-readiness claim, or replacement for crate-boundary documentation.

For protocol behavior, see `specification.md`.

For crate ownership and architecture boundaries, see `docs/CRATE_RESPONSIBILITIES.md`.

For conformance status, see `docs/CONFORMANCE.md`.

## Workspace Summary

| Crate | Status | Description |
| --- | --- | --- |
| `sar-cdc` | `implemented` | Content-defined chunking (CDC) algorithm and data model for SAR Protocol v1.0 Milestone 9a; stored CDC metadata is authoritative for parsing, while FASTCDC boundary-regeneration interoperability remains implementation-defined where the spec is incomplete. |
| `sar-cli` | `partial` | User-facing command-line interface over sar-archive, sar-core low-level APIs, and specialized feature crates for archive create/extract/list/verify/inspect/repair/version workflows, including metadata preservation/restoration controls and safe extraction policy. |
| `sar-compression` | `implemented` | Compression algorithm registry plus bounded stream encode/decode helpers. |
| `sar-core` | `partial` | Canonical SAR wire-format, status/error, limits, and low-level parse/write helper APIs. |
| `sar-archive` | `implemented` | High-level SAR archive reader/writer, verification, audit, transform orchestration, forward-only stream parser, conformance/profile validation, and archive-level recovery/repair APIs. |
| `sar-crypto` | `partial` | Hashing, AEAD, KMS parsing, key-provider abstraction, zeroizing secret helpers, and TLS_EXPORTER KMS mode types/context encoding (M10e). |
| `sar-delta` | `implemented` | Patch algorithm registry, patch generation, and patch application for STORE_PATCH, VCDIFF, and SAR BSDIFF v1. |
| `sar-fec` | `implemented` | Forward Error Correction codecs and metadata parsing for XOR and Reed-Solomon SAR FEC. |
| `sar-fragmentation` | `implemented` | Fragment semantic validation and reassembly logic (moved from sar-core in M11c). |
| `sar-loss-tolerant` | `implemented` | Pure policy helpers for LOSS_TOLERANT degraded reconstruction (added in M11c). No dependency on sar-core. |
| `sar-partition` | `deferred` | Partition/multi-volume archive support. Deliberately deferred in M11c. PartitionDescriptor and PARTITIONED_ARCHIVE remain in sar-core. |
| `sar-sparse` | `implemented` | Sparse extent validation and reconstruction logic (moved from sar-core in M11c). Wire-format parse/write remains in sar-core. |
| `sar-stream` | `implemented` | Stateful Streaming Mode session layer, stream transcript semantic validation, and optional exact-byte transcript recording. |
| `sar-transport` | `implemented` | Transport abstraction traits, deterministic in-memory TCP-like/QUIC-like harness over sar-stream, SAR-over-TCP binding (M10d), and SAR-over-QUIC binding (M10e, quic feature). |

## Crates

### `sar-cdc`

Path: `crates/sar-cdc`

Status: `implemented`

Content-defined chunking (CDC) algorithm and data model for SAR Protocol v1.0 Milestone 9a; stored CDC metadata is authoritative for parsing, while FASTCDC boundary-regeneration interoperability remains implementation-defined where the spec is incomplete.

#### Public functions

| Name | Signature | Description |
| --- | --- | --- |
| `algo_name` | `pub fn algo_name(id: u8) -> &'static str` | Display helper mapping known CDC IDs to short names; unknown/reserved IDs map to "unknown" |
| `chunk_data` | `pub fn chunk_data(data: &[u8], opts: &FastCdcOptions, max_chunks: usize) -> Result<Vec<CdcChunk>, CdcError>` | Chunk data using deterministic FASTCDC with explicit chunk-count bounds; local-profile behavior is not a portable cross-writer guarantee |
| `FastCdcOptions::validate` | `pub fn FastCdcOptions::validate(&self) -> Result<(), CdcError>` | Validates min/avg/max FASTCDC option ordering and floor constraints |
| `parse_cdc_map` | `pub fn parse_cdc_map(bytes: &[u8], max_records: usize) -> Result<CdcMap, CdcError>` | Parse stored CDC_MAP v1 TLV binary payload into CdcMap. Validates header (version, hash algo ID, flags, reserved, record_size, length), then parses all records. Does not regenerate FASTCDC boundaries. |
| `validate_cdc_algo_id` | `pub fn validate_cdc_algo_id(id: u8) -> Result<(), CdcError>` | Validates a CDC algorithm ID; returns Err for reserved or unsupported IDs |
| `validate_cdc_map` | `pub fn validate_cdc_map(map: &CdcMap, max_records: usize) -> Result<(), CdcError>` | Validates parsed CDC_MAP records (count limit and non-zero compressed_size) |
| `validate_cdc_map_bytes` | `pub fn validate_cdc_map_bytes(bytes: &[u8], max_records: usize) -> Result<(), CdcError>` | Validates CDC_MAP v1 TLV byte slice by delegating to parse_cdc_map; performs full structural validation including header, flags, reserved bytes, record_size, and length consistency |
| `validate_cdc_map_hash_algo_id` | `pub fn validate_cdc_map_hash_algo_id(id: u8) -> Result<(), CdcError>` | Validates a CDC_MAP Hash_Algorithm_ID: 0x30 (SHA-256) and 0x31 (BLAKE3) return Ok; 0x32 (SHA3-256, assigned but unsupported) returns Unsupported; all other values return ReservedValue |
| `validate_cdc_metadata` | `pub fn validate_cdc_metadata(meta: &CdcMetadata, logical_size: u64, max_chunks: usize) -> Result<(), CdcError>` | Validates CDC chunk metadata structure, contiguity, bounds, and chunk-count limits |
| `verify_cdc_map_record_hash` | `pub fn verify_cdc_map_record_hash(record: &CdcMapRecord, hash_algorithm_id: u8, archive_bytes: &[u8]) -> Result<bool, CdcError>` | Verify that a CDC_MAP record hash matches the bytes at \[Absolute_Offset, Absolute_Offset+Compressed_Size) in archive_bytes using the given hash algorithm (0x30=SHA-256, 0x31=BLAKE3). Uses checked arithmetic. Does not claim FASTCDC boundary-regeneration verification. |
| `write_cdc_map` | `pub fn write_cdc_map(map: &CdcMap) -> Result<Vec<u8>, CdcError>` | Serialize CdcMap into CDC_MAP v1 TLV binary payload with 16-byte header and 48-byte records |

#### Public types

| Name | Kind | Signature | Description |
| --- | --- | --- | --- |
| `CdcChunk` | `struct` | `pub struct CdcChunk { pub offset: u64, pub length: u64, pub hash: Option<[u8; 32]> }` | A single CDC chunk with offset, length, and optional SHA-256 hash |
| `CdcError` | `enum` |  | CDC-specific error type with variants: Unsupported, ReservedValue, Overflow, Bounds, Malformed, LimitExceeded, InvalidLength |
| `CdcMap` | `struct` | `pub struct CdcMap { pub hash_algorithm_id: u8, pub records: Vec<CdcMapRecord> }` | Parsed CDC_MAP TLV v1 content; includes Hash_Algorithm_ID from header |
| `CdcMapHeader` | `struct` | `pub struct CdcMapHeader { pub map_version: u8, pub hash_algorithm_id: u8, pub flags: u16, pub record_count: u32, pub record_size: u16 }` | CDC_MAP v1 header (16 bytes): Map_Version(1) + Hash_Algorithm_ID(1) + Flags(2) + Record_Count(4) + Record_Size(2) + Reserved(6). Makes CDC_MAP self-describing. |
| `CdcMapRecord` | `struct` | `pub struct CdcMapRecord { pub hash: [u8; 32], pub partition_id: u32, pub absolute_offset: u64, pub compressed_size: u32 }` | Single CDC_MAP v1 TLV record (48-byte layout: Hash 32B + Partition_ID u32 4B + Absolute_Offset u64 8B + Compressed_Size u32 4B) |
| `CdcMetadata` | `struct` | `pub struct CdcMetadata { pub algorithm_id: u8, pub min_size: u32, pub avg_size: u32, pub max_size: u32, pub chunks: Vec<CdcChunk> }` | Complete CDC metadata for a file |
| `FastCdcOptions` | `struct` | `pub struct FastCdcOptions { pub min_size: u32, pub avg_size: u32, pub max_size: u32 }` | FASTCDC chunking parameters (defaults: 2KiB/8KiB/64KiB; not standardized by the current spec) |

#### Public constants

| Name | Signature | Description |
| --- | --- | --- |
| `CDC_ALGO_CUSTOM_MAX` | `pub const CDC_ALGO_CUSTOM_MAX: u8 = 0xFF;` | Last custom/vendor CDC algorithm ID in the implementation-defined range |
| `CDC_ALGO_CUSTOM_MIN` | `pub const CDC_ALGO_CUSTOM_MIN: u8 = 0xF0;` | First custom/vendor CDC algorithm ID in the implementation-defined range |
| `CDC_ALGO_FASTCDC` | `pub const CDC_ALGO_FASTCDC: u8 = 0x02;` | FASTCDC required baseline algorithm |
| `CDC_ALGO_LITERAL` | `pub const CDC_ALGO_LITERAL: u8 = 0x00;` | Literal mode (payload is raw data, no chunking) |
| `CDC_CHUNK_MAX_DEFAULT` | `pub const CDC_CHUNK_MAX_DEFAULT: usize = 1_000_000;` | Default maximum accepted CDC chunk count for structural validation |
| `CDC_MAP_HEADER_SIZE` | `pub const CDC_MAP_HEADER_SIZE: usize = 16;` | Byte length of the CDC_MAP v1 header |
| `CDC_MAP_MAX_RECORDS_DEFAULT` | `pub const CDC_MAP_MAX_RECORDS_DEFAULT: usize = 1_000_000;` | Default maximum accepted CDC_MAP record count for structural validation |
| `CDC_MAP_RECORD_LEN` | `pub const CDC_MAP_RECORD_LEN: usize = 48;` | Byte length of a single CDC_MAP v1 record (32+4+8+4) |
| `CDC_MAP_V1_RECORD_SIZE` | `pub const CDC_MAP_V1_RECORD_SIZE: u16 = 48;` | Record_Size field value for CDC_MAP v1 (must match CDC_MAP_RECORD_LEN) |
| `CDC_MAP_VERSION_V1` | `pub const CDC_MAP_VERSION_V1: u8 = 0x01;` | Map_Version value for CDC_MAP v1 |
| `CDC_RECIPE_HASH_LEN` | `pub const CDC_RECIPE_HASH_LEN: usize = 32;` | Length of each recipe chunk hash in bytes |

### `sar-cli`

Path: `crates/sar-cli`

Status: `partial`

User-facing command-line interface over sar-archive, sar-core low-level APIs, and specialized feature crates for archive create/extract/list/verify/inspect/repair/version workflows, including metadata preservation/restoration controls and safe extraction policy.

No public-facing API items are listed for this crate.

### `sar-compression`

Path: `crates/sar-compression`

Status: `implemented`

Compression algorithm registry plus bounded stream encode/decode helpers.

#### Public functions

| Name | Signature | Description |
| --- | --- | --- |
| `CompressionAlgorithm::from_id` | `pub fn CompressionAlgorithm::from_id(id: u8) -> Result<Self, CompressionError>` | Resolve a registry ID into a compression algorithm. |
| `CompressionAlgorithm::id` | `pub const fn CompressionAlgorithm::id(self) -> u8` | Return the wire ID for a compression algorithm. |
| `CompressionAlgorithm::name` | `pub const fn CompressionAlgorithm::name(self) -> &str` | Return a human-readable algorithm name. |
| `decode_stream` | `pub fn decode_stream(algo_id: u8, input: &mut dyn Read, output: &mut dyn Write, options: DecompressionOptions) -> Result<(), CompressionError>` | Decode an encoded byte stream using the selected algorithm. |
| `encode_stream` | `pub fn encode_stream(algo_id: u8, input: &mut dyn Read, output: &mut dyn Write, options: CompressionOptions) -> Result<(), CompressionError>` | Encode a byte stream using the selected algorithm. |

#### Public types

| Name | Kind | Signature | Description |
| --- | --- | --- | --- |
| `CompressionAlgorithm` | `enum` | `pub enum CompressionAlgorithm { Store, Deflate, Zstd }` | Compression registry enum. |
| `CompressionError` | `enum` | `pub enum CompressionError { ... }` | Compression/decompression error type. |
| `CompressionOptions` | `struct` | `pub struct CompressionOptions { pub level: Option<u8> }` | Compression encode settings. |
| `DecompressionOptions` | `struct` | `pub struct DecompressionOptions { pub max_output_size: u64 }` | Decode-time output bound for decompression. |

#### Public constants

| Name | Signature | Description |
| --- | --- | --- |
| `COMP_ALGO_DEFLATE` | `pub const COMP_ALGO_DEFLATE: u8 = 0x01` | DEFLATE registry identifier. |
| `COMP_ALGO_STORE` | `pub const COMP_ALGO_STORE: u8 = 0x00` | STORE registry identifier. |
| `COMP_ALGO_ZSTD` | `pub const COMP_ALGO_ZSTD: u8 = 0x02` | ZSTD registry identifier. |

### `sar-core`

Path: `crates/sar-core`

Status: `partial`

Canonical SAR wire-format, status/error, limits, and low-level parse/write helper APIs.

#### Public functions

| Name | Signature | Description |
| --- | --- | --- |
| `classify_recovery_tlv_id` | `pub fn classify_recovery_tlv_id(type_id: u8) -> Result<(), SarError>` | Validate a recovery-TLV algorithm ID. |
| `compute_lfh_size` | `pub fn compute_lfh_size(flags: &GlobalFlags, lfh: &LocalFileHeader) -> Result<u64, SarError>` | Compute LFH size based on active flags and field presence. |
| `EntryMode::bits` | `pub const fn EntryMode::bits(self) -> u16` | Return the raw entry-mode wire bits. |
| `EntryMode::from_bits` | `pub const fn EntryMode::from_bits(bits: u16) -> Self` | Construct an entry mode from raw wire bits. |
| `EntryMode::is_compressed` | `pub const fn EntryMode::is_compressed(self) -> bool` | Return whether an entry is marked compressed. |
| `EntryMode::is_encrypted` | `pub const fn EntryMode::is_encrypted(self) -> bool` | Return whether an entry is marked encrypted. |
| `EntryMode::is_fragment` | `pub const fn EntryMode::is_fragment(self) -> bool` | Return whether an entry is marked fragment. |
| `EntryMode::is_last_fragment` | `pub const fn EntryMode::is_last_fragment(self) -> bool` | Return whether an entry is marked last fragment. |
| `EntryMode::is_loss_tolerant` | `pub const fn EntryMode::is_loss_tolerant(self) -> bool` | Return whether an entry permits degraded reconstruction. |
| `is_cdc_metadata_tlv_type` | `pub fn is_cdc_metadata_tlv_type(type_id: u8) -> bool` | Returns true for CDC metadata TLV IDs in range 0x40..=0x4F |
| `lfh_to_bytes` | `pub fn lfh_to_bytes(lfh: &LocalFileHeader, flags: GlobalFlags) -> Result<Vec<u8>, SarError>` | Serialize an LFH into on-wire bytes. |
| `LocalFileHeader::minimal_store` | `pub fn LocalFileHeader::minimal_store(name: Vec<u8>, payload_size: u64) -> Self` | Construct a minimal STORE LFH template. |
| `make_cdc_ext_provider_tlv` | `pub fn make_cdc_ext_provider_tlv(uri: &str, limits: &ResourceLimits) -> Result<Tlv, SarError>` | Builds inert CDC_EXT_PROVIDER metadata TLV from UTF-8 URI bytes |
| `make_cdc_map_tlv` | `pub fn make_cdc_map_tlv(map: &CdcMap, limits: &ResourceLimits) -> Result<Tlv, SarError>` | Serializes CdcMap into CDC_MAP TLV payload with metadata-size limits |
| `parse_cdc_ext_provider_tlv` | `pub fn parse_cdc_ext_provider_tlv(tlv: &Tlv, limits: &ResourceLimits) -> Result<CdcExtProviderMetadata, SarError>` | Parses CDC_EXT_PROVIDER as inert UTF-8 metadata only; no provider resolution/network access |
| `parse_central_dictionary` | `pub fn parse_central_dictionary(input: &[u8], flags: GlobalFlags, limits: &ResourceLimits) -> Result<(CentralDictionary, usize), SarError>` | Parse a central dictionary from bytes. |
| `parse_entry_cdc_map` | `pub fn parse_entry_cdc_map(tlvs: &[Tlv], limits: &ResourceLimits) -> Result<Option<CdcMap>, SarError>` | Extracts/parses first stored CDC_MAP TLV from metadata; does not regenerate FASTCDC boundaries |
| `parse_footer` | `pub fn parse_footer(input: &[u8]) -> Result<Footer, SarError>` | Parse a footer from bytes. |
| `parse_global_header` | `pub fn parse_global_header(input: &[u8], limits: &ResourceLimits) -> Result<(GlobalHeader, usize), SarError>` | Parse a global header from bytes. |
| `parse_lfh` | `pub fn parse_lfh(input: &[u8], flags: &GlobalFlags, limits: &ResourceLimits) -> Result<(LocalFileHeader, usize), SarError>` | Parse an LFH from bytes. |
| `parse_lfh_fec_value` | `pub fn parse_lfh_fec_value(algo_id: u8, fec_value: &[u8], limits: &ResourceLimits) -> Result<Option<FecSummary>, SarError>` | Parse and validate an LFH FEC value. |
| `parse_sparse_map` | `pub fn parse_sparse_map(bytes: &[u8], is_64bit: bool, limits: &ResourceLimits) -> Result<Vec<SparseExtent>, SarError>` | Parses a sparse map byte blob into a Vec&lt;SparseExtent&gt;. Enforces configured limits before allocating. |
| `parse_tlvs` | `pub fn parse_tlvs(input: &[u8], limits: &ResourceLimits) -> Result<Vec<Tlv>, SarError>` | Parse a sequence of TLV blocks with alignment checks. |
| `recipe_hashes` | `pub fn recipe_hashes(payload: &[u8]) -> Vec<[u8; 32]>` | Extracts ordered 32-byte hashes from a validated recipe payload |
| `ResourceLimits::allocation_len` | `pub fn ResourceLimits::allocation_len(&self, size: u64, label: &'static str) -> Result<usize, SarError>` | Checked u64-to-usize conversion with limit enforcement. All allocation size conversions go through this function. |
| `ResourceLimits::check_cdc_chunk_count` | `pub fn ResourceLimits::check_cdc_chunk_count(&self, count: usize) -> Result<(), SarError>` | Ensures CDC chunk/record count does not exceed max_cdc_chunk_count |
| `ResourceLimits::check_cdc_metadata_bytes` | `pub fn ResourceLimits::check_cdc_metadata_bytes(&self, bytes: usize) -> Result<(), SarError>` | Ensures CDC metadata byte size does not exceed max_cdc_metadata_bytes |
| `ResourceLimits::default` | `pub fn ResourceLimits::default() -> ResourceLimits` | Conservative safe defaults including max_cdc_chunk_count=1,000,000 and max_cdc_metadata_bytes=50 MiB. |
| `ResourceLimits::unlimited` | `pub fn ResourceLimits::unlimited() -> ResourceLimits` | All limits set to u64::MAX. Intended for test use only; do not use in production with untrusted input. |
| `SarError::status` | `pub const fn SarError::status(&self) -> SarStatus` | Map a SarError into its SAR status code. |
| `SarStatus::code` | `pub const fn SarStatus::code(self) -> i32` | Return the numeric SAR status code. |
| `SarStatus::name` | `pub const fn SarStatus::name(self) -> &str` | Return the registry constant name for a SAR status code. |
| `Tlv::encoded_len` | `pub fn Tlv::encoded_len(&self) -> usize` | Return encoded TLV size including alignment padding. |
| `validate_cdc_algo_id` | `pub fn validate_cdc_algo_id(id: u8) -> Result<(), SarError>` | Validates CDC algorithm ID and maps unsupported/reserved values to SarError |
| `validate_cdc_metadata_tlv` | `pub fn validate_cdc_metadata_tlv(tlv: &Tlv, limits: &ResourceLimits) -> Result<(), SarError>` | Validates CDC metadata TLVs structurally: CDC_MAP parsed, CDC_EXT_PROVIDER inert UTF-8, 0x42-0x4E reserved errors, CDC_CUSTOM parsed/preserved only |
| `validate_entry_mode_against_global` | `pub fn validate_entry_mode_against_global(global_flags: GlobalFlags, entry_mode: EntryMode) -> Result<(), SarError>` | Validate entry-mode bits against archive-wide flags. |
| `validate_global_flags` | `pub fn validate_global_flags(flags: GlobalFlags) -> Result<(), SarError>` | Validate archive-wide flag combinations. |
| `validate_lfh_fec_algo_id` | `pub fn validate_lfh_fec_algo_id(algo_id: u8) -> Result<(), SarError>` | Validate an LFH FEC algorithm ID. |
| `validate_recipe_payload` | `pub fn validate_recipe_payload(payload: &[u8], limits: &ResourceLimits) -> Result<usize, SarError>` | Structural recipe validation only (32-byte alignment + limits); not recipe-content verification |
| `validate_recovery_tlv` | `pub fn validate_recovery_tlv(type_id: u8, value: &[u8], limits: &ResourceLimits) -> Result<FecSummary, SarError>` | Validate a recovery TLV and summarize its metadata. |
| `write_central_dictionary` | `pub fn write_central_dictionary(cd: &CentralDictionary, flags: GlobalFlags) -> Result<Vec<u8>, SarError>` | Serialize a central dictionary. |
| `write_footer` | `pub fn write_footer(footer: Footer) -> [u8; 8]` | Serialize a footer. |
| `write_global_header` | `pub fn write_global_header(header: &GlobalHeader) -> Result<Vec<u8>, SarError>` | Serialize a global header. |
| `write_lfh` | `pub fn write_lfh(flags: &GlobalFlags, lfh: &LocalFileHeader) -> Result<Vec<u8>, SarError>` | Serialize an LFH. |
| `write_sparse_map` | `pub fn write_sparse_map(extents: &[SparseExtent], is_64bit: bool) -> Result<Vec<u8>, SarError>` | Serializes sparse extents to the LFH wire format. In 32-bit mode (is_64bit=false), fails closed with SarError::Overflow if any extent offset or length exceeds u32::MAX. In 64-bit mode, writes full u64 values. Never silently truncates. |
| `write_tlvs` | `pub fn write_tlvs(tlvs: &[Tlv]) -> Result<Vec<u8>, SarError>` | Encode TLV blocks with alignment padding. |

#### Public types

| Name | Kind | Signature | Description |
| --- | --- | --- | --- |
| `CdcAlgoId` | `struct` | `pub struct CdcAlgoId(pub u8)` | Typed wrapper for LFH CDC Algo ID with literal/recipe mode helpers |
| `CdcExtProviderMetadata` | `struct` | `pub struct CdcExtProviderMetadata { pub uri: String }` | Inert parsed CDC_EXT_PROVIDER metadata; URI only, no provider resolution/network access |
| `CentralDictionary` | `struct` | `pub struct CentralDictionary { pub version: u8, pub file_count: u64, pub partition_info: Option<(u16, u16)>, pub global_crc32: Option<u32>, pub metadata: Vec<Tlv>, pub offsets: Vec<u64> }` | Central dictionary structure. |
| `EntryCdcMetadata` | `struct` | `pub struct EntryCdcMetadata { pub algo_id: u8 }` | CDC algorithm ID from LFH CDC_SUPPORT field. |
| `EntryCompressionMetadata` | `struct` | `pub struct EntryCompressionMetadata { pub algo_id: u8 }` | Compression algorithm ID from LFH COMPRESSED field. |
| `EntryDeltaMetadata` | `struct` | `pub struct EntryDeltaMetadata { pub algo_id: u8, pub base_hash: [u8; 32] }` | Delta patch algorithm ID and base-file hash from LFH HAS_DELTA fields. |
| `EntryEncryptionMetadata` | `struct` | `pub struct EntryEncryptionMetadata { pub algo_id: u8, pub iv_nonce: [u8; 24] }` | Encryption algorithm ID and IV/nonce from LFH ENCRYPTED fields. |
| `EntryFecMetadata` | `struct` | `pub struct EntryFecMetadata { pub algo_id: u8 }` | FEC algorithm ID from LFH SELECTIVE_FEC field. |
| `EntryFragmentMetadata` | `struct` | `pub struct EntryFragmentMetadata { pub fragment_id: u32, pub fragment_index: u32, pub absolute_offset: u64, pub fragment_size: u32 }` | Fragment descriptor fields from LFH FILE_FRAGMENTATION global flag. |
| `EntryHashMetadata` | `struct` | `pub struct EntryHashMetadata { pub algo_id: u8, pub hash: [u8; 32] }` | Content hash algorithm ID and hash bytes from LFH DEDUPLICATION field. |
| `EntryKind` | `enum` | `pub enum EntryKind { RegularFile, Directory, Symlink, EmptyArea }` | Entry kind derived from EntryMode bits. RegularFile: default. Directory: IS_DIRECTORY set. Symlink: IS_SYMLINK set. EmptyArea: name is empty. |
| `EntryMode` | `struct` | `pub struct EntryMode { bits: u16 }` | Entry mode bit wrapper. |
| `EntryOwnerMetadata` | `struct` | `pub struct EntryOwnerMetadata { pub uid_gid: u32 }` | UID/GID ownership info decoded from the packed EXT_UID_GID u32 field (low 16 bits = UID, high 16 bits = GID, i.e. uid \| (gid &lt;&lt; 16)). Use uid() and gid() accessor methods. |
| `EntryPermissionMetadata` | `struct` | `pub struct EntryPermissionMetadata { pub mode: u16 }` | POSIX-style permission mode bits from LFH HAS_PERMS field. |
| `EntrySparseMetadata` | `struct` | `pub struct EntrySparseMetadata { pub logical_size: u64, pub extents: Vec<SparseExtent> }` | Sparse file map from LFH SPARSE_FILES global flag. |
| `EntryTimestampMetadata` | `struct` | `pub struct EntryTimestampMetadata { pub mtime: u64, pub atime: u64, pub ctime: u64 }` | Modification, access, and status-change timestamps (64-bit Unix seconds) from LFH EXT_TIME field. |
| `FecSummary` | `enum` | `pub enum FecSummary { Xor { ... }, ReedSolomon { ... } }` | Concise FEC metadata summary for LFH or recovery TLV inspection. |
| `FieldPresence` | `enum` | `pub enum FieldPresence<T> { Absent, PresentInactive(T), PresentActive(T) }` | Three-state presence model for LFH metadata fields. Absent: global flag not set, field physically missing from wire. PresentInactive: global flag set but entry mode bit unset. PresentActive: global flag set and semantically active. |
| `Footer` | `struct` | `pub struct Footer { pub cd_offset: u64 }` | Indexed-archive footer structure. |
| `GlobalFlags` | `struct` | `pub struct GlobalFlags: u32 { ... }` | Archive-wide flag bitset. |
| `GlobalHeader` | `struct` | `pub struct GlobalHeader { pub version: u8, pub flags_bytes: Vec<u8>, pub flags: GlobalFlags, pub partition_descriptor: Option<PartitionDescriptor>, pub kms: Option<KmsData> }` | Parsed SAR global header. |
| `KmsData` | `struct` | `pub struct KmsData { pub mode_id: u8, pub payload: Vec<u8> }` | Raw KMS extension bytes from the global header. |
| `LfhFragmentDescriptor` | `struct` | `pub struct LfhFragmentDescriptor { pub absolute_offset: u64, pub fragment_size: u32 }` | Fragment descriptor stored inline in a Local File Header. Records the absolute byte offset and declared byte size of this fragment's contribution within the fully reconstructed logical file. Replaces the former Option&lt;(u64, u32)&gt; tuple in LocalFileHeader.fragment_descriptor. |
| `LocalFileHeader` | `struct` | `pub struct LocalFileHeader { ... }` | Parsed SAR local file header. |
| `PartitionDescriptor` | `struct` | `pub struct PartitionDescriptor { ... }` | Optional 96-byte partition descriptor. |
| `ResourceLimits` | `struct` | `pub struct ResourceLimits { pub max_archive_size: u64, pub max_entry_count: u64, pub max_lfh_bytes: u64, pub max_path_bytes: u64, pub max_tlv_bytes: u64, pub max_tlv_count: u64, pub max_cd_bytes: u64, pub max_sparse_map_bytes: u64, pub max_sparse_descriptors: u64, pub max_fragment_count: u64, pub max_fragment_group_span: u64, pub max_loss_tolerant_gap: u64, pub max_fec_value_bytes: u64, pub max_recovery_protected_range: u64, pub max_repair_working_set: u64, pub max_decoded_entry_size: u64, pub max_in_memory_buffer: u64, pub max_total_pipeline_memory: u64, pub max_cdc_chunk_count: usize, pub max_cdc_metadata_bytes: usize, ... }` | Unified resource-limit configuration threaded through all parsing and reconstruction paths, including CDC map/recipe metadata limits. Configured limits are the primary protection against allocation-based DoS. |
| `SarError` | `enum` | `pub enum SarError { ... }` | Top-level SAR error type used across sar-core. |
| `SarStatus` | `enum` | `pub enum SarStatus { ... }` | SAR status and error registry values (`#\[repr(i32)\]`). |
| `SarStatusParseError` | `struct` | `pub struct SarStatusParseError(pub i32);` | Error returned when converting an unknown integer into SarStatus. |
| `SparseExtent` | `struct` | `pub struct SparseExtent { pub offset: u64, pub length: u64 }` | A single contiguous data extent within a sparse logical file. |
| `Tlv` | `struct` | `pub struct Tlv { pub type_id: u8, pub value: Vec<u8> }` | Global metadata TLV block. |

#### Public constants

| Name | Signature | Description |
| --- | --- | --- |
| `EntryMode::COMPRESSED` | `pub const EntryMode::COMPRESSED: u16 = 1 << 3` | Bit flag for compressed entry payloads. |
| `EntryMode::ENCRYPTED` | `pub const EntryMode::ENCRYPTED: u16 = 1 << 2` | Bit flag for encrypted entry payloads. |
| `EntryMode::FRAGMENT` | `pub const EntryMode::FRAGMENT: u16 = 1 << 5` | Bit flag for fragmented entries. |
| `EntryMode::LAST_FRAGMENT` | `pub const EntryMode::LAST_FRAGMENT: u16 = 1 << 6` | Bit flag for the final fragment in a group. |
| `EntryMode::LOSS_TOLERANT` | `pub const EntryMode::LOSS_TOLERANT: u16 = 1 << 7` | Bit flag for degraded loss-tolerant reconstruction. |
| `SUPPORTED_CD_VERSION` | `pub const SUPPORTED_CD_VERSION: u8 = 0x01` | Central dictionary version supported by the current implementation. |
| `TLV_CDC_CUSTOM` | `pub const TLV_CDC_CUSTOM: u8 = 0x4F;` | Implementation-defined CDC_CUSTOM metadata TLV ID |
| `TLV_CDC_EXT_PROVIDER` | `pub const TLV_CDC_EXT_PROVIDER: u8 = 0x41;` | CDC_EXT_PROVIDER metadata TLV ID (parsed inertly) |
| `TLV_CDC_MAP` | `pub const TLV_CDC_MAP: u8 = 0x40;` | CDC_MAP metadata TLV ID |
| `TLV_CDC_RESERVED_END` | `pub const TLV_CDC_RESERVED_END: u8 = 0x4E;` | Last reserved CDC metadata TLV ID |
| `TLV_CDC_RESERVED_START` | `pub const TLV_CDC_RESERVED_START: u8 = 0x42;` | First reserved CDC metadata TLV ID |
| `TLV_DATA_HASH_BLAKE3` | `pub const TLV_DATA_HASH_BLAKE3: u8 = 0x31;` | DATA_HASH/BLAKE3 TLV ID; explicitly not CDC metadata |

### `sar-archive`

Path: `crates/sar-archive`

Status: `implemented`

High-level SAR archive reader/writer, verification, audit, transform orchestration, forward-only stream parser, conformance/profile validation, and archive-level recovery/repair APIs.

#### Public functions

| Name | Signature | Description |
| --- | --- | --- |
| `ArchiveReader::audit` | `pub fn ArchiveReader<R>::audit(&mut self, options: ArchiveAuditOptions) -> Result<ArchiveAuditReport, SarError>` | Performs deterministic archive structural audit with explicit control-entry and payload policies. Default-safe mode rejects SESSION_CONTROL/nonzero OP_CODE; inert mode can preserve such entries as non-executed audit records. |
| `ArchiveReader::metadata` | `pub fn ArchiveReader<R>::metadata(&self) -> Option<ArchiveMetadata>` | Return parsed metadata after the header has been read. |
| `ArchiveReader::new` | `pub fn ArchiveReader<R>::new(reader: R) -> Result<Self, SarError>` | Create an archive reader with default limits. |
| `ArchiveReader::next_entry` | `pub fn ArchiveReader<R>::next_entry(&mut self) -> Result<Option<EntryReader>, SarError>` | Read the next sequential archive entry. |
| `ArchiveReader::read_all_logical_files` | `pub fn ArchiveReader<R>::read_all_logical_files(&mut self, allow_lossy: bool) -> Result<Vec<LogicalFile>, SarError>` | High-level bounded extraction: reads all entries, assembles fragment groups via reconstruct_fragments, applies sparse zero-fill via apply_sparse_reconstruction, and returns LogicalFile values. Resets the read cursor so it can be called even after prior next_entry calls. When allow_lossy is false (default), a missing fragment returns FragmentGap. When allow_lossy is true and LOSS_TOLERANT is set, degraded output is returned with LogicalFile::is_degraded = true. AEAD and format errors are never suppressed. Sparse Map on a non-zero fragment index returns InvalidMap immediately (never suppressed by allow_lossy). Sparse reconstruction uses LFH Uncompressed Size as the final logical file size; trailing holes are filled with 0x00. CRC32 is verified over fully reconstructed bytes (including holes) when PER_FILE_CRC is set; CrcMismatch is returned on failure. Empty Areas (Name Length == 0, IS_FRAGMENT == 0) are excluded from the returned list. EXPANSION-BOMB PROTECTION: a tiny stored payload combined with a huge Uncompressed Size and a sparse extent near the end (e.g. Uncompressed Size=1025, max_decoded_entry_size=1024, sparse map {offset=1024, length=1}, one stored byte) is rejected with SAR_ERR_LIMIT_EXCEEDED before any large allocation is attempted - not SAR_ERR_INVALID_MAP, because the sparse map is structurally valid. The same protection applies to fragmented sparse entries using fragment-0's Uncompressed Size. |
| `ArchiveReader::read_global_header` | `pub fn ArchiveReader<R>::read_global_header(&mut self) -> Result<GlobalHeader, SarError>` | Parse the archive global header and, for indexed archives, the CD/footer. |
| `ArchiveReader::verify` | `pub fn ArchiveReader<R>::verify(&mut self) -> Result<VerificationReport, SarError>` | Verify archive structure/index consistency and CDC metadata structural validity when CDC_SUPPORT is active. |
| `ArchiveReader::with_key_provider` | `pub fn ArchiveReader<R>::with_key_provider(self, key_provider: Box<dyn KeyProvider>) -> Self` | Attach a key provider for encrypted archive decoding. |
| `ArchiveReader::with_options` | `pub fn ArchiveReader<R>::with_options(reader: R, options: ArchiveReaderOptions) -> Result<Self, SarError>` | Create an archive reader with configurable decode limits. |
| `ArchiveRecoverySettings::default_rs` | `pub fn ArchiveRecoverySettings::default_rs() -> Self` | Return default Reed-Solomon archive-level recovery settings. |
| `ArchiveRecoverySettings::default_xor` | `pub fn ArchiveRecoverySettings::default_xor() -> Self` | Return default XOR archive-level recovery settings. |
| `ArchiveWriter::add_entry` | `pub fn ArchiveWriter<W>::add_entry(&mut self, entry: EntryInput) -> Result<EntryWritten, SarError>` | Add one archive entry. When the writer was created with CDC metadata, LFH cdc_algo_id is emitted as LITERAL_MODE (0x00) to keep CDC_SUPPORT archives internally consistent. |
| `ArchiveWriter::finish` | `pub fn ArchiveWriter<W>::finish(self) -> Result<ArchiveSummary, SarError>` | Finalize the archive and optionally write the CD/footer, including any configured Central Dictionary metadata. When archive_recovery is configured, finish() computes the protected byte range from the first Global Flags byte through the final byte before the Central Dictionary and emits one Central Dictionary RECOVERY TLV before writing the footer. |
| `ArchiveWriter::new` | `pub fn ArchiveWriter<W>::new(writer: W, options: ArchiveWriterOptions) -> Result<Self, SarError>` | Create a writer with STORE compression. |
| `ArchiveWriter::new_with_cd_metadata` | `pub fn ArchiveWriter<W>::new_with_cd_metadata(writer: W, options: ArchiveWriterOptions, cd_metadata: Vec<Tlv>) -> Result<Self, SarError>` | Create indexed writer with explicit CD metadata. CDC metadata auto-enables OPT_PRESENT and CDC_SUPPORT; normal entry APIs emit LFH cdc_algo_id = LITERAL_MODE for consistency. |
| `ArchiveWriter::new_with_compression` | `pub fn ArchiveWriter<W>::new_with_compression(writer: W, options: ArchiveWriterOptions, compression: CompressionSettings) -> Result<Self, SarError>` | Create a writer with explicit compression settings. |
| `ArchiveWriter::new_with_compression_and_key_provider` | `pub fn ArchiveWriter<W>::new_with_compression_and_key_provider(writer: W, options: ArchiveWriterOptions, compression: CompressionSettings, key_provider: Option<Box<dyn KeyProvider>>) -> Result<Self, SarError>` | Create a writer with compression and an optional key provider. |
| `ArchiveWriter::stream_state` | `pub fn ArchiveWriter<W>::stream_state(&self) -> StreamWriteState` | Returns current writer structural phase. |
| `ArchiveWriter::write_sparse_entry` | `pub fn ArchiveWriter<W>::write_sparse_entry(&mut self, name: &str, gathered_payload: &[u8], sparse: SparseWriteOptions) -> Result<(), SarError>` | Write a sparse entry. gathered_payload must have exactly sum(extent.length) bytes. sparse.logical_size is written to LFH Uncompressed Size. Requires ArchiveWriterOptions::sparse=true; returns Malformed otherwise. Validates extents (overlap, bounds, payload length mismatch) before writing. Applies compression/encryption/FEC from writer options. |
| `CompressionSettings::store` | `pub const fn CompressionSettings::store() -> Self` | Return STORE/default compression settings. |
| `EntryInput::file` | `pub fn EntryInput::file(name: impl Into<String>, payload: Vec<u8>) -> Self` | Ergonomic constructor for a regular file entry with default metadata. Use this instead of struct literal construction. |
| `FecSettings::default_rs` | `pub fn FecSettings::default_rs() -> Self` | Return default Reed-Solomon settings. |
| `FecSettings::default_xor` | `pub fn FecSettings::default_xor() -> Self` | Return default XOR settings. |
| `StreamArchiveParser::finalize_input` | `pub fn StreamArchiveParser::finalize_input(&mut self)` | Declare end-of-stream for truncated/error finalization. |
| `StreamArchiveParser::new` | `pub fn StreamArchiveParser::new() -> Self` | Create parser with default options. |
| `StreamArchiveParser::push_bytes` | `pub fn StreamArchiveParser::push_bytes(&mut self, chunk: &[u8]) -> Result<(), SarError>` | Append bytes to parser input buffer. |
| `StreamArchiveParser::state` | `pub fn StreamArchiveParser::state(&self) -> StreamParseState` | Return current parser phase. |
| `StreamArchiveParser::step` | `pub fn StreamArchiveParser::step(&mut self) -> Result<StreamStep<StreamEvent>, SarError>` | Run one deterministic parser step. |
| `StreamArchiveParser::with_key_provider` | `pub fn StreamArchiveParser::with_key_provider(self, key_provider: Box<dyn KeyProvider>) -> Self` | Attach key provider for encrypted entries. |
| `StreamArchiveParser::with_options` | `pub fn StreamArchiveParser::with_options(options: ArchiveReaderOptions) -> Self` | Create parser with explicit options. |
| `validate_archive_profile` | `pub fn validate_archive_profile(meta: &ArchiveMetadata, profile: ComplianceProfile) -> ProfileReport` | Validate archive metadata against the currently implemented compliance-profile rules. |

#### Public types

| Name | Kind | Signature | Description |
| --- | --- | --- | --- |
| `ArchiveAuditEntryKind` | `enum` | `pub enum ArchiveAuditEntryKind { OrdinaryEntry, InertOpcodeEntry, InertSessionControl }` | Per-entry audit classification for ordinary entries, inert control entries, or rejected entries. |
| `ArchiveAuditEntryReport` | `struct` | `pub struct ArchiveAuditEntryReport { pub lfh_offset: u64, pub entry_mode_bits: u16, pub op_code: u8, pub session_control: bool, pub kind: ArchiveAuditEntryKind, pub payload_size: u64, pub uncompressed_size: u64, pub payload_status: ArchiveAuditPayloadStatus, ... }` | Per-entry archive audit report. |
| `ArchiveAuditMode` | `enum` | `pub enum ArchiveAuditMode { Indexed, NoIndex }` | Audit mode classification for ordinary archive entries versus explicitly inert control/session-shaped entries. |
| `ArchiveAuditOptions` | `struct` | `pub struct ArchiveAuditOptions { pub control_entry_policy: ControlEntryPolicy, pub payload_policy: PayloadAuditPolicy, pub include_inert_payload_bytes: bool }` | Options for deterministic archive audit, including control-entry handling and payload-audit policy. |
| `ArchiveAuditPayloadStatus` | `enum` | `pub enum ArchiveAuditPayloadStatus { Decoded, Skipped, Unavailable, Failed }` | Per-entry payload verification status reported by archive audit. |
| `ArchiveAuditRecoverySummary` | `struct` | `pub struct ArchiveAuditRecoverySummary { pub tlv_count: u32, pub tlv_type_ids: Vec<u8> }` | Summary of archive-level recovery metadata discovered during audit. |
| `ArchiveAuditReport` | `struct` | `pub struct ArchiveAuditReport { pub mode: ArchiveAuditMode, pub global_flags: u32, pub entry_count_seen: u64, pub entries: Vec<ArchiveAuditEntryReport>, pub footer_present: bool, pub central_dictionary_present: bool, pub cd_footer_valid: bool, pub recovery: Option<ArchiveAuditRecoverySummary> }` | Deterministic archive audit report containing entry reports, recovery summary, and aggregate counts/status. |
| `ArchiveMetadata` | `struct` | `pub struct ArchiveMetadata { pub global_header: GlobalHeader, pub central_dictionary: Option<CentralDictionary> }` | Parsed archive metadata summary. |
| `ArchiveReader` | `struct` | `pub struct ArchiveReader<R> { ... }` | Streaming archive reader over a seekable source. |
| `ArchiveReaderOptions` | `struct` | `pub struct ArchiveReaderOptions { pub limits: ResourceLimits }` | Reader-side decode limits. |
| `ArchiveRecoverySettings` | `struct` | `pub struct ArchiveRecoverySettings { pub algo_id: u8, pub config0: u8, pub config1: u8, pub symbol_size: u32 }` | Writer-side archive-level RECOVERY TLV configuration. The writer uses these settings during finish() to generate one Central Dictionary RECOVERY TLV over the protected range from the first Global Flags byte through the final byte before the Central Dictionary. |
| `ArchiveSummary` | `struct` | `pub struct ArchiveSummary { pub entry_count: u64, pub archive_size: u64, pub indexed: bool }` | Writer finish summary. |
| `ArchiveWriter` | `struct` | `pub struct ArchiveWriter<W> { ... }` | Archive writer with compression and optional encryption support. |
| `ArchiveWriterOptions` | `struct` | `pub struct ArchiveWriterOptions { pub no_index: bool, pub sparse: bool, pub encryption: Option<EncryptionSettings>, pub fec: Option<FecSettings>, pub archive_recovery: Option<ArchiveRecoverySettings>, pub with_path: bool, pub with_permissions: bool, pub with_uid_gid: bool, pub with_timestamps: bool, pub with_per_file_crc: bool, pub with_content_hash: bool, pub with_symlinks: bool, pub with_delta: bool, pub lfh_size_field_policy: LfhSizeFieldPolicy }` | Writer options for indexing, sparse, encryption, Selective FEC, and archive-level Recovery TLV generation. archive_recovery is indexed-only, sets HAS_GLOBAL_EC plus OPT_PRESENT before the Global Header is emitted, and rejects no_index=true. LFH Selective FEC and archive-level Recovery TLV are separate wire-format features. New in M11a: with_path, with_permissions, with_uid_gid, with_timestamps, with_per_file_crc, with_content_hash, with_symlinks enable corresponding GlobalFlags and allow the matching EntryInput fields. New in M11a.1: lfh_size_field_policy controls LFH size-field layout policy (`Auto`/`Force32`/`Force64`). |
| `ComplianceProfile` | `enum` | `pub enum ComplianceProfile { MinimalInteroperableArchive, StaticArchive, Package, StreamPackage, Backup, Telemetry, LiveMedia, Standard }` | Current archive compliance-profile selector. |
| `CompressionSettings` | `struct` | `pub struct CompressionSettings { pub algo_id: u8, pub level: Option<u8> }` | Archive writer compression settings. |
| `ControlEntryPolicy` | `enum` | `pub enum ControlEntryPolicy { Reject, PreserveInert }` | Policy controlling how archive audit handles entries with SESSION_CONTROL set or nonzero OP_CODE. |
| `DeltaWriteOptions` | `struct` | `pub struct DeltaWriteOptions { pub algorithm: PatchAlgoId, pub base: Vec<u8>, pub delta_base_hash: [u8; 32] }` | Per-entry delta write options. Set on EntryInput.delta when with_delta=true. algorithm selects the patch algorithm; base provides the base bytes for VCDIFF/BSDIFF; delta_base_hash is a non-zero 32-byte opaque identity written to LFH. All-zero delta_base_hash is rejected for VCDIFF and BSDIFF at write time. |
| `EncryptionSettings` | `struct` | `pub struct EncryptionSettings { pub algo_id: u8, pub kms_params: KmsParams }` | Archive writer encryption settings. |
| `EntryInput` | `struct` | `pub struct EntryInput { pub name: String, pub payload: Vec<u8>, pub kind: Option<EntryKind>, pub path: Option<String>, pub permissions: Option<u16>, pub uid_gid: Option<u32>, pub timestamps: Option<EntryTimestampMetadata>, pub is_hidden: bool, pub stream_id: Option<u16>, pub sequence_no: Option<u16>, pub file_crc32: Option<u32>, pub content_hash: Option<EntryHashMetadata> }` | Input payload for archive writing. Use EntryInput::file(name, payload) for simple regular file entries. All metadata fields are optional and default to absent. Metadata fields are validated fail-closed against enabled GlobalFlags at add_entry time; missing flag causes SarError::FlagConflict. Symlink entries require HAS_SYMLINKS and a UTF-8 payload target. |
| `EntryMetadata` | `struct` | `pub struct EntryMetadata { pub name: String, pub flags: EntryMode, pub compression: CompressionAlgorithm, pub sparse_extents: Vec<SparseExtent>, pub file_crc32: Option<u32>, pub content_hash: Option<[u8; 32]>, pub cdc_algo_id: Option<u8>, pub kind: EntryKind, pub path: Option<String>, pub symlink_target: Option<String>, pub permissions: Option<u16>, pub uid_gid: Option<u32>, pub timestamps: Option<EntryTimestampMetadata>, pub is_hidden: bool, pub compression_presence: FieldPresence<EntryCompressionMetadata>, pub encryption_presence: FieldPresence<EntryEncryptionMetadata>, pub fec_presence: FieldPresence<EntryFecMetadata>, pub fragment_presence: FieldPresence<EntryFragmentMetadata>, pub cdc: Option<EntryCdcMetadata>, pub delta: Option<EntryDeltaMetadata>, pub sparse: Option<EntrySparseMetadata>, pub content_hash_meta: Option<EntryHashMetadata>, pub raw_entry_mode: u16, pub path_presence: FieldPresence<String>, pub permissions_presence: FieldPresence<EntryPermissionMetadata>, pub owner_presence: FieldPresence<EntryOwnerMetadata>, pub timestamps_presence: FieldPresence<EntryTimestampMetadata> }` | Complete metadata for a parsed archive entry. Includes all LFH fields, M11a presence-model fields, and M11b filesystem metadata FieldPresence fields. FieldPresence&lt;T&gt; fields distinguish: Absent (global flag not set), PresentInactive(T) (global flag set but semantically inactive), PresentActive(T) (global flag set and active). New in M11b: path_presence (HAS_PATH), permissions_presence (HAS_PERMS), owner_presence (EXT_UID_GID), timestamps_presence (EXT_TIME), and symlink_target decoded from symlink payload bytes. Symlink target payload is validated as strict UTF-8; invalid UTF-8 is rejected (not lossy). Zero values are preserved and never collapsed to Absent. kind is derived from entry mode bits. raw_entry_mode preserves the raw wire value. |
| `EntryReader` | `struct` | `pub struct EntryReader { pub header: LocalFileHeader, pub payload: Vec<u8>, pub metadata: EntryMetadata }` | Decoded entry payload plus metadata. |
| `EntryWritten` | `struct` | `pub struct EntryWritten { pub lfh_offset: u64, pub total_bytes: u64 }` | Summary returned after writing one entry. |
| `FecSettings` | `struct` | `pub struct FecSettings { pub algo_id: u8, pub config0: u8, pub config1: u8, pub symbol_size: u32 }` | Writer-side Selective FEC configuration. |
| `LfhSizeFieldPolicy` | `enum` | `pub enum LfhSizeFieldPolicy { Auto, Force32, Force64 }` | Archive-writer LFH size-field policy: Auto (default 32-bit with promotion before header emission), Force32 (fail closed if any size exceeds u32::MAX), Force64 (always emit 64-bit fields and set global SIZE_64BIT). |
| `LogicalFile` | `struct` | `pub struct LogicalFile { pub name: String, pub fragment_id: Option<u32>, pub data: Vec<u8>, pub is_degraded: bool }` | A fully reconstructed logical file returned by ArchiveReader::read_all_logical_files. For fragmented entries, fragments have been assembled at their absolute offsets. For sparse entries, holes are zero-filled. is_degraded is true when the payload is incomplete due to LOSS_TOLERANT-permitted missing fragments. |
| `PayloadAuditPolicy` | `enum` | `pub enum PayloadAuditPolicy { MetadataOnly, DecodeWhenKeysAvailable, RequireDecode }` | Policy controlling whether archive audit validates only structure, decodes when keys/bases are available, or requires payload decoding. |
| `ProfileReport` | `struct` | `pub struct ProfileReport { pub compliant: bool, pub findings: Vec<String> }` | Report returned by profile validation. |
| `SparseWriteOptions` | `struct` | `pub struct SparseWriteOptions { pub logical_size: u64, pub extents: Vec<SparseExtent> }` | Options for writing a sparse entry via ArchiveWriter::write_sparse_entry. logical_size is the full apparent file size including holes and is written to LFH Uncompressed Size. extents must be ordered, non-overlapping, and within logical_size bounds. |
| `StreamArchiveParser` | `struct` | `pub struct StreamArchiveParser { ... }` | Forward-only stateless SAR byte-stream parser with partial-input support. |
| `StreamArchiveSummary` | `struct` | `pub struct StreamArchiveSummary { pub entry_count: u64, pub flags: GlobalFlags, pub indexed: bool }` | Per-archive completion summary emitted in stream mode. |
| `StreamEvent` | `enum` | `pub enum StreamEvent { GlobalHeader(Box<GlobalHeader>), Entry(Box<EntryReader>), ArchiveComplete(StreamArchiveSummary) }` | Events emitted by StreamArchiveParser. |
| `StreamParseState` | `enum` | `pub enum StreamParseState { NeedGlobalHeader, NeedLocalFileHeader, NeedPayload, TransformingEntry, EntryReady, NeedCentralDictionaryOrFooter, ArchiveComplete, Error }` | Explicit forward-only parser phases. |
| `StreamStep` | `enum` | `pub enum StreamStep<T> { NeedMore { needed: Option<usize> }, Ready(T), Complete }` | Deterministic incremental parser step result. |
| `StreamWriteState` | `enum` | `pub enum StreamWriteState { NeedLocalFileHeader, NeedPayload, EntryReady, NeedCentralDictionaryOrFooter, ArchiveComplete, Error }` | Structural archive writer phases for stream-oriented emission. |
| `VerificationReport` | `struct` | `pub struct VerificationReport { pub valid: bool, pub entry_count: u64, pub indexed: bool, pub cdc_entry_count: u64, pub cdc_support: bool }` | Verification summary including CDC support and number of entries carrying cdc_algo_id |

### `sar-crypto`

Path: `crates/sar-crypto`

Status: `partial`

Hashing, AEAD, KMS parsing, key-provider abstraction, zeroizing secret helpers, and TLS_EXPORTER KMS mode types/context encoding (M10e).

#### Public functions

| Name | Signature | Description |
| --- | --- | --- |
| `aead_decrypt` | `pub fn aead_decrypt(algo_id: u8, key: &SecretBytes, iv_nonce_field: &[u8; 24], aad: &[u8], ciphertext_with_tag: &[u8]) -> Result<Vec<u8>, SarCryptoError>` | Decrypt and authenticate ciphertext plus tag. |
| `aead_encrypt` | `pub fn aead_encrypt(algo_id: u8, key: &SecretBytes, iv_nonce_field: &[u8; 24], aad: &[u8], plaintext: &[u8]) -> Result<Vec<u8>, SarCryptoError>` | Encrypt plaintext and append the AEAD tag. |
| `blake3_hash` | `pub fn blake3_hash(data: &[u8]) -> [u8; 32]` | Compute a BLAKE3 hash. |
| `build_aead_aad` | `pub fn build_aead_aad(global_flags_section: &[u8], lfh_bytes: &[u8]) -> Vec<u8>` | Build AAD bytes from the global-flag section and LFH bytes. |
| `ct_eq` | `pub fn ct_eq(a: &[u8], b: &[u8]) -> bool` | Constant-time equality check for two byte slices of equal length. |
| `generate_nonce` | `pub fn generate_nonce(algo_id: u8, field: &mut [u8; 24]) -> Result<(), SarCryptoError>` | Fill the 24-byte on-wire nonce field for the selected algorithm. |
| `global_header_aad_bytes` | `pub fn global_header_aad_bytes(magic: &[u8; 4], version: u8, flags_bytes: &[u8]) -> Vec<u8>` | Build AAD bytes for a SAR global header. |
| `hash_data` | `pub fn hash_data(algo_id: u8, data: &[u8]) -> Result<Vec<u8>, SarCryptoError>` | Compute a one-shot hash for a selected algorithm. |
| `new_hasher` | `pub fn new_hasher(algo_id: u8) -> Result<Box<dyn Hasher>, SarCryptoError>` | Construct a streaming hasher for a selected algorithm. |
| `parse_kms_payload` | `pub fn parse_kms_payload(mode_id: u8, payload: &[u8]) -> Result<KmsParams, SarCryptoError>` | Parse raw KMS payload bytes for a given mode. |
| `resolve_cek` | `pub fn resolve_cek(provider: &dyn KeyProvider, context: &KmsContext) -> Result<SecretBytes, SarCryptoError>` | Resolve a content-encryption key from a KeyProvider and KMS context. |
| `serialize_kms_payload` | `pub fn serialize_kms_payload(params: &KmsParams) -> Vec<u8>` | Serialize KMS parameters into on-wire payload bytes. |
| `sha256` | `pub fn sha256(data: &[u8]) -> [u8; 32]` | Compute a SHA-256 hash. |
| `validate_encr_algo_id` | `pub fn validate_encr_algo_id(id: u8) -> Result<(), SarCryptoError>` | Validate an encryption algorithm identifier. |
| `validate_kms_mode_id` | `pub fn validate_kms_mode_id(id: u8) -> Result<(), SarCryptoError>` | Validate a KMS mode identifier. |
| `validate_nonce_field` | `pub fn validate_nonce_field(algo_id: u8, field: &[u8; 24], strict: bool) -> Result<(), SarCryptoError>` | Validate the 24-byte on-wire nonce field for the selected algorithm. |

#### Public types

| Name | Kind | Signature | Description |
| --- | --- | --- | --- |
| `Argon2Params` | `struct` | `pub struct Argon2Params { pub variant: u8, pub version: u8, pub salt: Vec<u8>, pub memory_cost_kib: u32, pub time_cost: u32, pub parallelism: u16, pub derived_key_length: u16 }` | Parsed Argon2 KMS payload. |
| `AsymmetricRecipient` | `struct` | `pub struct AsymmetricRecipient { pub recipient_id: Vec<u8>, pub wrapped_key: Vec<u8> }` | One recipient entry for asymmetric key wrapping. |
| `AsymmetricWrapParams` | `struct` | `pub struct AsymmetricWrapParams { pub wrap_algo_id: u8, pub recipients: Vec<AsymmetricRecipient> }` | Parsed asymmetric-wrap KMS payload. |
| `Hasher` | `trait` | `pub trait Hasher { fn update(&mut self, data: &[u8]); fn finalize(self: Box<Self>) -> Vec<u8>; fn algorithm_id(&self) -> u8; }` | Streaming hasher abstraction. |
| `KeyProvider` | `trait` | `pub trait KeyProvider: Send + Sync { ... }` | Trait that supplies passwords or CEKs to archive readers and writers. |
| `KmsContext` | `struct` | `pub struct KmsContext { pub mode_id: u8, pub params: KmsParams }` | Key-resolution context passed to KeyProvider implementations. |
| `KmsParams` | `enum` | `pub enum KmsParams { Pbkdf2(Pbkdf2Params), Argon2(Argon2Params), AsymmetricWrap(AsymmetricWrapParams) }` | Parsed KMS payload variant. |
| `Pbkdf2Params` | `struct` | `pub struct Pbkdf2Params { pub prf_algo_id: u8, pub salt: Vec<u8>, pub iterations: u32, pub derived_key_length: u16 }` | Parsed PBKDF2 KMS payload. |
| `SarCryptoError` | `enum` | `pub enum SarCryptoError { ... }` | Error type for crypto primitives and KMS helpers. |
| `SecretBytes` | `type_alias` | `pub type SecretBytes = Zeroizing<Vec<u8>>` | Zeroizing container for secret byte buffers. |
| `SecretString` | `type_alias` | `pub type SecretString = Zeroizing<String>` | Zeroizing container for secret strings. |

#### Public constants

| Name | Signature | Description |
| --- | --- | --- |
| `AEAD_KEY_SIZE` | `pub const AEAD_KEY_SIZE: usize = 32` | AEAD key size in bytes. |
| `AEAD_TAG_SIZE` | `pub const AEAD_TAG_SIZE: usize = 16` | AEAD tag size in bytes. |
| `ARGON2_VARIANT_D` | `pub const ARGON2_VARIANT_D: u8 = 0x01` | Argon2d variant identifier. |
| `ARGON2_VARIANT_I` | `pub const ARGON2_VARIANT_I: u8 = 0x02` | Argon2i variant identifier. |
| `ARGON2_VARIANT_ID` | `pub const ARGON2_VARIANT_ID: u8 = 0x03` | Argon2id variant identifier. |
| `ENCR_AES256_CBC` | `pub const ENCR_AES256_CBC: u8 = 0x03` | Assigned AES-256-CBC encryption identifier (not implemented in archive flows). |
| `ENCR_AES256_GCM` | `pub const ENCR_AES256_GCM: u8 = 0x01` | AES-256-GCM encryption identifier. |
| `ENCR_CHACHA20` | `pub const ENCR_CHACHA20: u8 = 0x02` | Assigned ChaCha20 identifier (not implemented in archive flows). |
| `ENCR_CHACHA20_POLY1305` | `pub const ENCR_CHACHA20_POLY1305: u8 = 0x05` | Assigned ChaCha20-Poly1305 identifier (not implemented in archive flows). |
| `ENCR_PLAINTEXT` | `pub const ENCR_PLAINTEXT: u8 = 0x00` | Plaintext/no-encryption identifier. |
| `ENCR_XCHACHA20_POLY` | `pub const ENCR_XCHACHA20_POLY: u8 = 0x04` | XChaCha20-Poly1305 encryption identifier. |
| `HASH_BLAKE3` | `pub const HASH_BLAKE3: u8 = 0x31` | BLAKE3 hash identifier. |
| `HASH_SHA256` | `pub const HASH_SHA256: u8 = 0x30` | SHA-256 hash identifier. |
| `HASH_SHA3_256` | `pub const HASH_SHA3_256: u8 = 0x32` | Assigned SHA3-256 hash identifier (not implemented). |
| `KMS_ARGON2` | `pub const KMS_ARGON2: u8 = 0x02` | Argon2 KMS mode identifier. |
| `KMS_ASYMMETRIC_WRAP` | `pub const KMS_ASYMMETRIC_WRAP: u8 = 0x03` | Asymmetric-wrap KMS mode identifier. |
| `KMS_PBKDF2` | `pub const KMS_PBKDF2: u8 = 0x01` | PBKDF2 KMS mode identifier. |
| `PBKDF2_PRF_HMAC_SHA256` | `pub const PBKDF2_PRF_HMAC_SHA256: u8 = 0x01` | PBKDF2 HMAC-SHA256 PRF identifier. |
| `PBKDF2_PRF_HMAC_SHA3_256` | `pub const PBKDF2_PRF_HMAC_SHA3_256: u8 = 0x03` | Assigned PBKDF2 HMAC-SHA3-256 PRF identifier. |
| `PBKDF2_PRF_HMAC_SHA512` | `pub const PBKDF2_PRF_HMAC_SHA512: u8 = 0x02` | Assigned PBKDF2 HMAC-SHA512 PRF identifier. |
| `TAG_SIZE` | `pub const TAG_SIZE: usize = 16` | AEAD tag size in bytes. |

### `sar-delta`

Path: `crates/sar-delta`

Status: `implemented`

Patch algorithm registry, patch generation, and patch application for STORE_PATCH, VCDIFF, and SAR BSDIFF v1.

#### Public functions

| Name | Signature | Description |
| --- | --- | --- |
| `apply_bsdiff` | `pub fn apply_bsdiff(base: &[u8], patch: &[u8], expected_target_size: u64, limits: &BsdiffLimits) -> Result<Vec<u8>, PatchError>` | Applies a SAR BSDIFF v1 patch (`SARBSD01`) to the supplied base bytes. Validates the 32-byte header (magic, block lengths, new file size), reads uncompressed Control/Diff/Extra blocks, applies control triples using sign-magnitude decoding, and returns the reconstructed target. Base reads beyond the end of base use 0x00. Caller supplies base explicitly; no lookup is performed. |
| `apply_store_patch` | `pub fn apply_store_patch(patch_payload: &[u8], expected_len: u64) -> Result<Vec<u8>, PatchError>` | Applies STORE_PATCH (identity transform): returns the patch payload as the reconstructed target if its length equals expected_len, otherwise returns PatchFailed. No base reads, no instruction stream, no external dictionary. |
| `apply_vcdiff` | `pub fn apply_vcdiff(base: &[u8], patch: &[u8], expected_target_size: u64, limits: &VcdiffLimits) -> Result<Vec<u8>, PatchError>` | Applies a VCDIFF (RFC 3284) patch to the supplied base bytes. Validates the 4-byte magic header, decodes windows and instruction tables, and reconstructs the target output. Supports VCD_SOURCE and VCD_TARGET address spaces; ADD, COPY, and RUN instructions; default code table. Caller supplies base explicitly; no lookup is performed. |
| `generate_bsdiff_patch` | `pub fn generate_bsdiff_patch(base: &[u8], target: &[u8], limits: &BsdiffLimits) -> Result<Vec<u8>, PatchError>` | Generates a valid SARBSD01 patch using a single control triple. Output is accepted by apply_bsdiff. O(target.len()) memory. Fails closed on limit violations. |
| `generate_vcdiff_patch` | `pub fn generate_vcdiff_patch(base: &[u8], target: &[u8], limits: &VcdiffLimits) -> Result<Vec<u8>, PatchError>` | Generates a valid RFC 3284 VCDIFF ADD-only stream from target bytes. base is accepted for API symmetry. Output is accepted by apply_vcdiff. O(target.len()) memory. Fails closed on limit violations. |
| `patch_algo_name` | `pub fn patch_algo_name(id: u8) -> &'static str` | Returns a display name string for any raw patch algorithm byte. |
| `validate_patch_algo_id` | `pub fn validate_patch_algo_id(id: u8) -> Result<PatchAlgoId, PatchError>` | Validates a raw patch algorithm byte against the SAR registry. Returns ReservedValue for 0x04-0xEF, Unsupported for 0xF0-0xFF, and PatchAlgoId for assigned IDs 0x00-0x03. Does not apply any patch. |

#### Public types

| Name | Kind | Signature | Description |
| --- | --- | --- | --- |
| `BsdiffLimits` | `struct` | `pub struct BsdiffLimits { pub max_patch_size: u64, pub max_control_bytes: u64, pub max_diff_bytes: u64, pub max_extra_bytes: u64, pub max_control_triples: usize, pub max_output_size: u64 }` | Resource limits for BSDIFF patch application. All sizes in bytes. max_output_size=0 defers to the max_decoded_entry_size from ResourceLimits. |
| `PatchAlgoId` | `enum` | `pub enum PatchAlgoId { StorePatch, Vcdiff, Bsdiff, ZstdPatch, Custom(u8) }` | SAR patch algorithm identifiers from spec ?8.4. Custom(u8) covers 0xF0-0xFF. STORE_PATCH, VCDIFF, and BSDIFF application are implemented; ZSTD_PATCH remains unsupported. |
| `PatchError` | `enum` | `pub enum PatchError { Unsupported, ReservedValue, PatchFailed(&'static str), BaseMissing(&'static str), LimitExceeded(&'static str) }` | Local error type for patch algorithm registry validation and application. Mapped to SarError at the sar-core boundary. PatchFailed for malformed data or size mismatch. BaseMissing when explicit base bytes are required but absent. LimitExceeded for ResourceLimits violations. |
| `VcdiffLimits` | `struct` | `pub struct VcdiffLimits { pub max_patch_size: u64, pub max_window_count: usize, pub max_instruction_count: usize, pub max_output_size: u64 }` | Resource limits for VCDIFF patch application. All sizes in bytes. max_output_size=0 defers to the max_decoded_entry_size from ResourceLimits. |

#### Public constants

| Name | Signature | Description |
| --- | --- | --- |
| `PATCH_ALGO_BSDIFF` | `pub const PATCH_ALGO_BSDIFF: u8 = 0x02;` | Algorithm ID for BSDIFF (0x02). Assigned; application implemented as SAR BSDIFF v1 (`SARBSD01`) with uncompressed Control/Diff/Extra blocks. Explicit base bytes required via ArchiveReaderOptions.delta_base. Legacy BSDIFF40 decode is unsupported. |
| `PATCH_ALGO_CUSTOM_MAX` | `pub const PATCH_ALGO_CUSTOM_MAX: u8 = 0xFF;` | Maximum custom patch algorithm ID (0xFF). Custom range: 0xF0-0xFF; all return Unsupported. |
| `PATCH_ALGO_CUSTOM_MIN` | `pub const PATCH_ALGO_CUSTOM_MIN: u8 = 0xF0;` | Minimum custom patch algorithm ID (0xF0). Custom range: 0xF0-0xFF; all return Unsupported. |
| `PATCH_ALGO_STORE_PATCH` | `pub const PATCH_ALGO_STORE_PATCH: u8 = 0x00;` | Algorithm ID for STORE_PATCH (0x00). Assigned; application implemented (STORE_PATCH pass). |
| `PATCH_ALGO_VCDIFF` | `pub const PATCH_ALGO_VCDIFF: u8 = 0x01;` | Algorithm ID for VCDIFF (0x01). Assigned; application implemented per RFC 3284 (M9b Delta pass). Explicit base bytes required via ArchiveReaderOptions.delta_base. |
| `PATCH_ALGO_ZSTD_PATCH` | `pub const PATCH_ALGO_ZSTD_PATCH: u8 = 0x03;` | Algorithm ID for ZSTD_PATCH (0x03). Assigned optional; application unsupported in M9b. |

### `sar-fec`

Path: `crates/sar-fec`

Status: `implemented`

Forward Error Correction codecs and metadata parsing for XOR and Reed-Solomon SAR FEC.

#### Public functions

| Name | Signature | Description |
| --- | --- | --- |
| `parse_fec_value` | `pub fn parse_fec_value(algo_id: u8, value_data: &[u8]) -> Result<FecMeta, FecError>` | Parse and validate an FEC value, returning structured metadata. |
| `parse_rs_meta` | `pub fn parse_rs_meta(data: &[u8]) -> Result<RsMeta, FecError>` | Parse Reed-Solomon metadata from an encoded FEC value. |
| `parse_xor_meta` | `pub fn parse_xor_meta(data: &[u8]) -> Result<XorMeta, FecError>` | Parse XOR metadata from an encoded FEC value. |
| `RsCodec::from_fec_value` | `pub fn RsCodec::from_fec_value(data: &[u8]) -> Result<Self, FecError>` | Construct a codec by reading config bytes from an encoded FEC value. |
| `RsCodec::new` | `pub fn RsCodec::new(k: u8, parity_count: u8, symbol_size: u32) -> Result<Self, FecError>` | Construct a Reed-Solomon codec. |
| `validate_fec_algo_id` | `pub fn validate_fec_algo_id(algo_id: u8) -> Result<(), FecError>` | Validate an FEC algorithm ID. |
| `validate_rs_fec_value` | `pub fn validate_rs_fec_value(data: &[u8]) -> Result<(), FecError>` | Validate Reed-Solomon FEC bytes without performing recovery. |
| `validate_xor_fec_value` | `pub fn validate_xor_fec_value(data: &[u8]) -> Result<(), FecError>` | Validate XOR FEC bytes without performing recovery. |
| `XorCodec::from_fec_value` | `pub fn XorCodec::from_fec_value(data: &[u8]) -> Result<Self, FecError>` | Construct a codec by reading config bytes from an encoded FEC value. |
| `XorCodec::new` | `pub fn XorCodec::new(stripe_size: u8, block_size_index: u8) -> Result<Self, FecError>` | Construct an XOR codec. |

#### Public types

| Name | Kind | Signature | Description |
| --- | --- | --- | --- |
| `Erasure` | `struct` | `pub struct Erasure { pub index: u64 }` | Explicit erasure position used during recovery. |
| `FecCodec` | `trait` | `pub trait FecCodec { fn algorithm_id(&self) -> u8; fn encode_recovery(...); fn recover(...); fn validate(...); }` | Trait implemented by FEC codecs. |
| `FecError` | `enum` | `pub enum FecError { ... }` | FEC encode/decode/validation error type. |
| `FecMeta` | `enum` | `pub enum FecMeta { Xor(XorMeta), Rs(RsMeta) }` | Parsed FEC metadata variants. |
| `FecOptions` | `struct` | `pub struct FecOptions;` | Algorithm-specific FEC encode options placeholder. |
| `FecRecoverInput` | `struct` | `pub struct FecRecoverInput<'a> { ... }` | Input bundle for FEC recovery. |
| `FecValue` | `struct` | `pub struct FecValue { pub algo_id: u8, pub data: Vec<u8> }` | Encoded FEC value including config, metadata, and parity. |
| `RsCodec` | `struct` | `pub struct RsCodec { ... }` | Reed-Solomon FEC codec. |
| `RsMeta` | `struct` | `pub struct RsMeta { pub k: u8, pub parity_count: u8, pub symbol_size: u32, pub original_protected_len: u64, pub group_count: u32, pub parity_data_len: usize }` | Parsed Reed-Solomon metadata summary. |
| `XorCodec` | `struct` | `pub struct XorCodec { ... }` | XOR FEC codec. |
| `XorMeta` | `struct` | `pub struct XorMeta { pub stripe_size: u8, pub block_size: u32, pub original_protected_len: u64, pub stripe_count: u32, pub parity_data_len: usize }` | Parsed XOR metadata summary. |

#### Public constants

| Name | Signature | Description |
| --- | --- | --- |
| `FEC_ALGO_REED_SOLOMON` | `pub const FEC_ALGO_REED_SOLOMON: u8 = 0x11` | Reed-Solomon FEC algorithm ID. |
| `FEC_ALGO_RS` | `pub const FEC_ALGO_RS: u8 = 0x11` | Reed-Solomon FEC algorithm ID alias. |
| `FEC_ALGO_XOR` | `pub const FEC_ALGO_XOR: u8 = 0x14` | XOR FEC algorithm ID. |
| `FEC_ALGO_XOR` | `pub const FEC_ALGO_XOR: u8 = 0x14` | XOR FEC algorithm ID alias. |

### `sar-fragmentation`

Path: `crates/sar-fragmentation`

Status: `implemented`

Fragment semantic validation and reassembly logic (moved from sar-core in M11c).

#### Public functions

| Name | Signature | Description |
| --- | --- | --- |
| `reconstruct_fragments` | `pub fn reconstruct_fragments(frags: Vec<FragmentEntry>, logical_size: u64, limits: &FragmentLimits) -> Result<(Vec<u8>, bool), FragmentError>` | Reassembles fragment payloads in order. Returns (data, is_degraded). Validates duplicate fragment indexes, payload-size agreement, index gaps, missing LAST_FRAGMENT, descriptor byte-range gaps (initial/middle/tail), and LOSS_TOLERANT degraded-output policy with max_loss_tolerant_gap. |
| `validate_fragment_group` | `pub fn validate_fragment_group(frags: &[FragmentEntry], logical_size: u64, limits: &FragmentLimits) -> Result<(), FragmentError>` | Validates fragment count/group-span limits, descriptor bounds, and descriptor non-overlap. |

#### Public types

| Name | Kind | Signature | Description |
| --- | --- | --- | --- |
| `FragmentDescriptor` | `struct` | `pub struct FragmentDescriptor { pub absolute_offset: u64, pub fragment_size: u32 }` | Per-fragment extent descriptor. |
| `FragmentEntry` | `struct` | `pub struct FragmentEntry { pub fragment_index: u32, pub is_last_fragment: bool, pub is_loss_tolerant: bool, pub descriptor: FragmentDescriptor, pub payload: Vec<u8> }` | Decoded fragment with payload and metadata. |
| `FragmentError` | `enum` | `pub enum FragmentError` | Error type for fragment operations. Variants: InvalidMap, Bounds, FragmentGap, LimitExceeded, Overflow. M11c-cp additions: PayloadSizeMismatch (payload length does not match descriptor.fragment_size, always fatal) and DuplicateIndex (duplicate fragment_index in group, always fatal even with LOSS_TOLERANT). |
| `FragmentLimits` | `struct` | `pub struct FragmentLimits { pub max_fragment_count: usize, pub max_fragment_group_span: u64, pub max_decoded_entry_size: u64, pub max_loss_tolerant_gap: u64, pub max_allocation_bytes: u64 }` | Resource limits for fragment operations. |

### `sar-loss-tolerant`

Path: `crates/sar-loss-tolerant`

Status: `implemented`

Pure policy helpers for LOSS_TOLERANT degraded reconstruction (added in M11c). No dependency on sar-core.

#### Public functions

| Name | Signature | Description |
| --- | --- | --- |
| `classify_recovery` | `pub fn classify_recovery(has_gap: bool, failed: bool) -> RecoveryStatus` | Maps gap/failure booleans to RecoveryStatus. |
| `gap_degraded_output_permitted` | `pub fn gap_degraded_output_permitted(is_loss_tolerant: bool) -> bool` | Returns whether a missing-fragment gap may produce degraded output. Requires LOSS_TOLERANT flag. |

#### Public types

| Name | Kind | Signature | Description |
| --- | --- | --- | --- |
| `RecoveryStatus` | `enum` | `pub enum RecoveryStatus` | Reconstruction outcome. Variants: Complete, Degraded, Failed. |

### `sar-partition`

Path: `crates/sar-partition`

Status: `deferred`

Partition/multi-volume archive support. Deliberately deferred in M11c. PartitionDescriptor and PARTITIONED_ARCHIVE remain in sar-core.

No public-facing API items are listed for this crate.

### `sar-sparse`

Path: `crates/sar-sparse`

Status: `implemented`

Sparse extent validation and reconstruction logic (moved from sar-core in M11c). Wire-format parse/write remains in sar-core.

#### Public functions

| Name | Signature | Description |
| --- | --- | --- |
| `apply_sparse_reconstruction` | `pub fn apply_sparse_reconstruction(payload: &[u8], extents: &[SparseExtent], logical_size: u64, limits: &SparseLimits) -> Result<Vec<u8>, SparseError>` | Scatter/gather reconstruction of logical file from payload and extent map; validates payload-length agreement because payload bytes are available. |
| `validate_sparse_extents` | `pub fn validate_sparse_extents(extents: &[SparseExtent], logical_size: u64, limits: &SparseLimits) -> Result<(), SparseError>` | Validates descriptor-count limits, non-zero length, sorted order, non-overlap, bounds within logical size, and arithmetic overflow safety. Does not validate payload-length agreement. |

#### Public types

| Name | Kind | Signature | Description |
| --- | --- | --- | --- |
| `SparseError` | `enum` | `pub enum SparseError` | Error type for sparse operations. Variants: InvalidMap, Truncated, LimitExceeded, Overflow. |
| `SparseExtent` | `struct` | `pub struct SparseExtent { pub offset: u64, pub length: u64 }` | One sparse data region (offset + length). |
| `SparseLimits` | `struct` | `pub struct SparseLimits { pub max_sparse_map_bytes: usize, pub max_sparse_descriptors: usize, pub max_decoded_entry_size: u64, pub max_allocation_bytes: u64 }` | Resource limits for sparse operations. |

### `sar-stream`

Path: `crates/sar-stream`

Status: `implemented`

Stateful Streaming Mode session layer, stream transcript semantic validation, and optional exact-byte transcript recording.

#### Public functions

| Name | Signature | Description |
| --- | --- | --- |
| `validate_stream_transcript` | `pub fn validate_stream_transcript(bytes: &[u8]) -> Result<StreamTranscriptValidationReport, SarError>` | Validate a serialized stream transcript with default validation options. |
| `validate_stream_transcript_with_options` | `pub fn validate_stream_transcript_with_options(bytes: &[u8], options: &StreamTranscriptValidationOptions) -> Result<StreamTranscriptValidationReport, SarError>` | Strict transcript semantic validation for NO_INDEX primary stream bytes, with optional exact-byte recording to caller-selected path/sink. |
| `validate_stream_transcript_with_sink` | `pub fn validate_stream_transcript_with_sink<W: Write + ?Sized>(bytes: &[u8], sink: &mut W) -> Result<StreamTranscriptValidationReport, SarError>` | Validate a serialized stream transcript while optionally recording exact input bytes to a transcript sink. |

#### Public types

| Name | Kind | Signature | Description |
| --- | --- | --- | --- |
| `SessionManager` | `struct` | `pub struct SessionManager { ... }` | In-memory Stateful Streaming Mode manager that binds Stream IDs to Session UUIDs, validates SESSION_* semantics, and emits actions/events without performing transport or filesystem I/O. |
| `StreamTranscriptValidationOptions` | `struct` | `pub struct StreamTranscriptValidationOptions { pub recording: TranscriptRecording }` | Options controlling stream transcript validation and optional transcript recording. |
| `StreamTranscriptValidationReport` | `struct` | `pub struct StreamTranscriptValidationReport { pub entry_count: u64 }` | Report returned after stream transcript validation, including validated entry count. |
| `TranscriptRecording` | `enum` | `pub enum TranscriptRecording { Disabled, Path { path: PathBuf, overwrite: bool } }` | Output mode for optional exact-byte stream transcript recording. |

### `sar-transport`

Path: `crates/sar-transport`

Status: `implemented`

Transport abstraction traits, deterministic in-memory TCP-like/QUIC-like harness over sar-stream, SAR-over-TCP binding (M10d), and SAR-over-QUIC binding (M10e, quic feature).

#### Public functions

| Name | Signature | Description |
| --- | --- | --- |
| `connect_quic` | `pub async fn connect_quic(server_name: &str, addr: SocketAddr, config: QuicClientConfig) -> Result<QuicSarConnection, SarError>` | Async QUIC client entry point. Establishes a QUIC connection to a QuicSarListener. Returns a ready-to-use QuicSarConnection. |
| `QuicSarConnection::accept_stream` | `pub async fn accept_stream(&self) -> Result<QuicSarStream, SarError>` | Accept the next incoming bidirectional QUIC stream on this connection. |
| `QuicSarConnection::export_keying_material` | `pub fn export_keying_material(&self, label: &[u8], context: &[u8], len: usize) -> Result<Vec<u8>, SarError>` | Derive TLS exporter material from the active QUIC/TLS session using the given label, context, and output length. Used for TLS_EXPORTER SAR-AEAD key derivation. |
| `QuicSarConnection::feed_stream_bytes` | `pub fn feed_stream_bytes(&mut self, stream_id: TransportStreamId, bytes: &[u8]) -> Result<Vec<TransportAction>, SarError>` | Feed bytes for a specific QUIC stream into the M10c QUIC-policy harness. Returns transport actions. Control-frame bytes are buffered; call flush_pending_control_frames to send. |
| `QuicSarConnection::flush_pending_control_frames` | `pub async fn flush_pending_control_frames(&mut self, stream: &mut SendStream) -> Result<(), SarError>` | Flush any control-frame bytes buffered by feed_stream_bytes to the QUIC send stream. |
| `QuicSarConnection::open_stream` | `pub async fn open_stream(&self) -> Result<QuicSarStream, SarError>` | Open a new outgoing bidirectional QUIC stream on this connection. |
| `QuicSarListener::accept` | `pub async fn accept(&self) -> Result<QuicSarConnection, SarError>` | Accept the next incoming QUIC connection. |
| `QuicSarListener::bind` | `pub async fn bind(config: QuicServerConfig) -> Result<QuicSarListener, SarError>` | Bind a QUIC listener on an OS-assigned port. Returns a ready-to-accept listener. |
| `QuicSarStream::stream_id` | `pub fn stream_id(&self) -> TransportStreamId` | Returns the local TransportStreamId for this QUIC stream. Used with feed_stream_bytes. |
| `TcpSarConnection::accept` | `pub fn accept(stream: TcpStream, config: TcpTransportConfig) -> Result<TcpSarConnection<TcpStream>, SarError>` | Accept an already-connected TcpStream from a TcpListener::accept call. |
| `TcpSarConnection::close` | `pub fn close(&mut self) -> Result<(), SarError>` | Close the connection gracefully. Marks the connection as closed, finalises the transport stream, and flushes the write buffer. Subsequent calls are no-ops. |
| `TcpSarConnection::connect` | `pub fn connect(addr: SocketAddr, config: TcpTransportConfig) -> Result<TcpSarConnection<TcpStream>, SarError>` | Connect to a remote SAR-over-TCP server. Returns a ready-to-use blocking connection. |
| `TcpSarConnection::from_stream` | `pub fn from_stream(stream: S, config: TcpTransportConfig) -> Result<TcpSarConnection<S>, SarError>` | Construct a connection from any Read+Write stream. Used for in-memory or mock testing. |
| `TcpSarConnection::process_available` | `pub fn process_available(&mut self, now_ms: Option<u64>) -> Result<Vec<TransportAction>, SarError>` | Read one bounded chunk from the stream, feed to TCP policy, serialize outbound control frames, and return actions. Checks inactivity watchdog when now_ms is Some. Returns StreamClosed on EOF. |
| `TcpSarConnection::write_all_sar_bytes` | `pub fn write_all_sar_bytes(&mut self, bytes: &[u8]) -> Result<(), SarError>` | Write raw SAR archive bytes to the TCP connection. Caller constructs valid SAR bytes. Capped at write_buffer_size per call. |

#### Public types

| Name | Kind | Signature | Description |
| --- | --- | --- | --- |
| `InMemoryTransport` | `struct` | `pub struct InMemoryTransport { ... }` | Deterministic in-memory transport model implementing TCP-like and QUIC-like policy behaviors.  M10i adds with_key_provider() for TLS_EXPORTER post-binding AEAD enforcement. |
| `QuicClientConfig` | `struct` | `pub struct QuicClientConfig { pub trust: QuicClientTrust, pub transport: QuicTransportConfig, pub alpn_protocols: Vec<Vec<u8>> }` | Configuration for a QUIC client connection. Combines client trust, transport limits, and optional ALPN protocols. |
| `QuicClientTrust` | `enum` | `pub enum QuicClientTrust { CustomCaDer(Vec<u8>), InsecureSkipVerifyForTestsOnly }` | Client certificate trust configuration. CustomCaDer is the only production-safe variant. InsecureSkipVerifyForTestsOnly is test-only and must never be used in production. |
| `QuicPolicy` | `struct` | `pub struct QuicPolicy;` | Marker for QUIC-like concurrent-stream transport policy model. |
| `QuicSarConnection` | `struct` | `pub struct QuicSarConnection { ... }` | A single QUIC connection that multiplexes multiple SAR sessions. Active SAR Stream IDs are unique per connection. Duplicate SESSION_INIT for an active Stream ID fails closed with SAR_ERR_STREAM_STATE. |
| `QuicSarListener` | `struct` | `pub struct QuicSarListener { ... }` | QUIC listener that accepts multiple concurrent QuicSarConnections. Built on quinn 0.11 + rustls 0.23 + tokio 1. |
| `QuicSarStream` | `struct` | `pub struct QuicSarStream { ... }` | A single bidirectional QUIC stream with its local TransportStreamId. Used to identify the stream in QuicSarConnection::feed_stream_bytes. |
| `QuicServerConfig` | `struct` | `pub struct QuicServerConfig { pub identity: QuicServerIdentity, pub transport: QuicTransportConfig, pub alpn_protocols: Vec<Vec<u8>> }` | Configuration for a QuicSarListener. Combines server identity, transport limits, and optional ALPN protocols. |
| `QuicServerIdentity` | `struct` | `pub struct QuicServerIdentity { pub cert_chain_der: Vec<Vec<u8>>, pub private_key_der: Vec<u8> }` | Explicit TLS server identity for QUIC connections. Carries DER-encoded certificate chain and DER-encoded private key. Private key is never logged or transmitted. |
| `QuicTransportConfig` | `struct` | `pub struct QuicTransportConfig { pub max_connections: usize, pub max_quic_streams_per_connection: usize, pub max_active_sar_streams_per_connection: usize, pub max_control_streams_per_sar_session: usize, pub max_buffered_bytes: usize, pub max_read_chunk: usize, pub max_outbound_buffer_bytes: usize, pub max_cert_chain_bytes: usize, pub max_private_key_bytes: usize, pub pq_policy: TlsPqPolicy }` | Per-connection QUIC transport limits and policy settings. All limits are enforced before allocation. pq_policy controls TLS key agreement policy per Section 18.6.7. |
| `SarTransportBinding` | `trait` | `pub trait SarTransportBinding { binding_kind, open_transport_stream, feed_bytes, close_transport_stream, reset_transport_stream }` | Abstract transport-binding interface for opening streams, feeding bytes, and producing policy actions. |
| `TcpPolicy` | `struct` | `pub struct TcpPolicy;` | Marker for TCP-like non-interleaved transport policy model. |
| `TcpSarConnection` | `struct` | `pub struct TcpSarConnection<S>` | SAR-over-TCP connection binding. Generic over any Read+Write stream. Uses a single TransportStreamId(0). Drives M10c TCP policy. Uses std::net blocking I/O; no async runtime, no TLS, no QUIC. |
| `TcpTransportConfig` | `struct` | `pub struct TcpTransportConfig { pub transport: TransportConfig, pub read_buffer_size: usize, pub write_buffer_size: usize }` | Configuration for a TcpSarConnection. Extends TransportConfig with per-call read/write buffer size limits enforced before allocation. |
| `TlsPqPolicy` | `enum` | `pub enum TlsPqPolicy { ClassicalAllowed, PreferPq, RequirePqOrHybrid, RequirePqOnly }` | TLS key agreement policy for SAR-over-QUIC connections (Section 18.6.7). Controls which TLS key agreement algorithm classes are offered and accepted. Default is ClassicalAllowed when the TLS stack does not support PQ or hybrid groups. RequirePqOrHybrid and RequirePqOnly fail closed with SAR_ERR_UNSUPPORTED when PQ/hybrid key agreement is unavailable. |
| `TransportAction` | `enum` | `pub enum TransportAction { ... }` | Abstract action/event output from transport policy evaluation. |
| `TransportBindingKind` | `enum` | `pub enum TransportBindingKind { Tcp, Quic, InMemory }` | Transport binding family discriminator for TCP-like, QUIC-like, and in-memory harness modes. |
| `TransportConfig` | `struct` | `pub struct TransportConfig { ... }` | Configures transport-level stream limits, buffering bounds, status/ack queues, and heartbeat/watchdog timing. |
| `TransportHarness` | `struct` | `pub struct TransportHarness { ... }` | Convenience deterministic harness that accumulates transport actions for tests and integration scaffolding. |
| `TransportStreamId` | `struct` | `pub struct TransportStreamId(pub u64);` | Opaque transport-stream identifier used by the harness policy layer. |
| `TransportStreamState` | `enum` | `pub enum TransportStreamState { Idle, AwaitingGlobalHeader, AwaitingSessionInit, Active, Closing, Closed, Reset, Rejected }` | Transport-stream state machine for policy/harness behavior. |

#### Public constants

| Name | Signature | Description |
| --- | --- | --- |
| `STREAM_ID` | `pub const STREAM_ID: TransportStreamId = TransportStreamId(0);` | The single fixed transport-stream ID used by all TCP connections (one SAR session at a time, no byte-interleaving). |
